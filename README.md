# Digio_Test_XO
## 📺 วิดีโอสาธิตการใช้งาน

- [ดูวิดีโอสอนการใช้งานระบบ](https://drive.google.com/drive/folders/1BRDbpwPFRS20YSZdGcNReXCDgeg296vx?usp=drive_link)

ระบบเกม XO แบบปรับขนาดได้  
- ผู้เล่นสามารถเลือกขนาดกระดานตั้งแต่ **3×3 ถึง 15×15**  
- รองรับการเล่นทั้ง **PVP (ผู้เล่น vs ผู้เล่น)** และ **PVE (ผู้เล่น vs Bot AI)**  
- มีระดับความยากของ AI 3 ระดับ: **EASY / NORMAL / HARD** (เฉพาะโหมด PVE)  
- สามารถดู **ประวัติการเล่น (Game History)** และ **ประวัติการเดินหมากแต่ละตา (Replay History)** ได้  
- แสดงตำแหน่งการชนะ (Winning Line) ได้อย่างชัดเจน  

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์

### Backend (Spring Boot)

```bash
```

### Frontend (React)

```bash
```

---

## วิธีออกแบบโปรแกรม

### 🗂️ ออกแบบคลาสและแอตทริบิวต์

ระบบนี้ออกแบบฐานข้อมูล 3 ตารางหลัก:  

1. **games** → เก็บข้อมูลเกม XO  
2. **histories** → เก็บข้อมูลประวัติการเดินหมากแต่ละตา  
3. **positions** → เก็บตำแหน่งที่ชนะบนกระดาน  

📌 [รายละเอียดตาราง]

### ชื่อตาราง: `games`

| คอลัมน์ | ชนิดข้อมูล | เงื่อนไข | คำอธิบาย | ตัวอย่างข้อมูล |
|----------|------------|----------|-----------|-----------|
| gameId | INT | PK | รหัสเกม | 1 |
| tableSize | INT | NN | ขนาดตาราง (เช่น 3, 5, 7 …) | 5 |
| winNum | INT | NN | จำนวนช่องที่ต้องเรียงเพื่อชนะ | 4 |
| gameStatus | VARCHAR(50) | NN | สถานะเกม เช่น `HUMAN_VS_BOT`, `HUMAN_VS_HUMAN` | HUMAN_VS_HUMAN |
| playerX | VARCHAR(100) | NN | ผู้เล่นฝั่ง X | Player |
| playerO | VARCHAR(100) | NN | ผู้เล่นฝั่ง O | Bot |
| endResult | VARCHAR(50) | NN | ผลลัพธ์ เช่น `X_WIN`, `O_WIN`, `DRAW` | DRAW |
| winText | VARCHAR(1) |  | ตัวอักษรผู้ชนะ (X/O) | X |
| bot_difficulty | VARCHAR(50) | NULL | ระดับบอท (EASY / NORMAL / HARD) | NORMAL |
| play_date | CALENDER |  | วันที่เล่นเกม | 2025-10-04 |
| first_turn | VARCHAR(1) | NN | ผู้เล่นที่เริ่มก่อน (X หรือ O) | X |

---

### ชื่อตาราง: `histories`

| คอลัมน์ | ชนิดข้อมูล | เงื่อนไข | คำอธิบาย | ตัวอย่างข้อมูล |
|----------|------------|----------|-----------|-----------|
| history_id | INT | PK | รหัสประวัติการเดิน | 10 |
| game_id | INT | FK | รหัสเกม | 1 |
| turn_num | INT | NN | หมายเลขตาที่เล่น (1, 2, …) | 3 |
| `col` | INT | NN | ตำแหน่งคอลัมน์ที่ลง | 2 |
| `row` | INT | NN | ตำแหน่งแถวที่ลง | 3 |
| position_id | INT | FK | รหัสตำแหน่ง | 7 |
| text | VARCHAR(1) | NN | ตัวอักษรที่ลง (X หรือ O) | X |

---

### ชื่อตาราง: `positions`

| คอลัมน์ | ชนิดข้อมูล | เงื่อนไข | คำอธิบาย | ตัวอย่างข้อมูล |
|----------|------------|----------|-----------|-----------|
| position_id | INT | PK | รหัสตำแหน่งที่ชนะ | 7 |
| game_id | INT | FK | รหัสเกม | 1 |
| `col` | INT | NN | ตำแหน่งคอลัมน์ที่ชนะ | 2 |
| `row` | INT | NN | ตำแหน่งแถวที่ชนะ | 3 |

---

## ⚙️ โครงสร้างโปรเจกต์

โปรเจกต์นี้ประกอบด้วย **Backend (Spring Boot)** และ **Frontend React (ใช้ Node.js) ร่วมกับ Express**  
ติดต่อกันผ่าน REST API  

### 📌 Backend – Spring Boot
ใช้สำหรับประมวลผลเกม XO และจัดการข้อมูลกับฐานข้อมูล  

**Dependencies ที่ใช้**
- **Spring Web** → สำหรับสร้าง REST API และสื่อสารกับ Frontend  
- **Spring Data JPA** → จัดการ ORM เชื่อมกับฐานข้อมูล ลดการเขียน SQL  
- **MySQL Driver** → ใช้เชื่อมต่อกับฐานข้อมูล MySQL  
- **Lombok** → ลด boilerplate code (Getter / Setter / Constructor)  

### 📌 Frontend – React
ใช้สำหรับแสดงผลเกม XO และประวัติการเล่นในรูปแบบ UI ที่ใช้งานง่าย  

**Libraries ที่ใช้**
- **React Router** → จัดการเส้นทางการนำทาง เช่น `HomePage`, `HistoryPage`, `ReplayPage`, `PvpPage`, `PvePage`  
- **Axios** → ใช้เรียก REST API จาก Backend  
- **Express (BFF - Backend For Frontend)** → ทำงานเป็นตัวกลางระหว่าง React และ Spring Boot API  

---

## 🚀 การทำงานหลักของระบบ

ภาพรวมลำดับเหตุการณ์ของเกม XO (ตั้งแต่เริ่มจนจบ)

### 1. ผู้ใช้เริ่มเกม
กำหนดค่าเริ่มต้นได้ 3 อย่าง:
- **ขนาดตาราง** เช่น 3×3, 5×5
- **จำนวนตัวเรียงเพื่อชนะ (ถ้าเป็นตาราง 4×4, 5×5)**   
- **โหมด** (ผู้เล่น vs ผู้เล่น หรือ ผู้เล่น vs Bot)
- **ถ้าเลือกเล่นผู้เล่น** vs Bot สามารถเลือกระดับ (EASY / NORMAL / HARD)

เมื่อสร้างเกมระบบจะส่งไปหน้าเล่นเกม  
สถานะเริ่มต้นของเกมคือ `กำลังดำเนินการ`

### 2. ผู้เล่นเดินหมาก
- ผู้เล่นเลือกตำแหน่ง (แถว, คอลัมน์) และตัวหมาก (X หรือ O)
- ระบบตรวจสอบความถูกต้อง เช่น ช่องว่าง, ลำดับตาที่ถูกต้อง

### 3. ตรวจสอบผลลัพธ์
หลังการเดินทุกครั้ง ระบบจะตรวจ:
- มีการเรียงครบจำนวนที่กำหนดหรือไม่
- กระดานเต็มหรือไม่

หากมีผู้ชนะ → ระบุว่า `ผู้ชนะ X` หรือ `ผู้ชนะ O`  
หากกระดานเต็มแต่ไม่มีผู้ชนะ → ระบุว่า `เสมอ`

### 4. สิ้นสุดเกม → บันทึกลงฐานข้อมูล
เมื่อเกมจบ ระบบจึงจะ:
- บันทึก `การเล่นเกม` พร้อมผลลัพธ์
- บันทึก `ประวัติการเดินหมากแต่ละตา` ทั้งหมดของเกมนั้น
- บันทึก `ตำแหน่งที่ชนะ` ถ้าเสมอจะไม่บันทึก

### 5. ดูย้อนหลัง / Replay
- ผู้ใช้สามารถเรียกดู **ประวัติการเดินหมากทั้งหมด** 
- ระบบจะส่งข้อมูล `ประวัติการเดินหมากแต่ละตา` และ `ตำแหน่งที่ชนะ` ให้ Frontend นำไปแสดงเป็น Replay
- ระบบจะส่งพิกัดชุดที่ชนะไป เพื่อใช้ในการไฮไลต์เส้นชนะ

---

## 💡 อัลกอริทึมบอท

บอทคิดจาก **2 ไอเดียหลัก**:

### 1. จุดตัด (Axis Intersection)
เลือกช่องที่ "อยู่บนเส้นทางชนะได้หลายเส้นพร้อมกัน" มากที่สุด  
→ ยิ่งผ่านหลายเส้น ยิ่งมีโอกาสทำเส้นทางชนะ/ปิดเกมได้มากกว่า

### 2. คะแนนความน่าจะเป็น (Probabilistic Weight)
ให้ "คะแนน" กับทุกช่องว่างจากมุมมอง *บุก* และ *กัน* แล้วเลือกช่องที่คะแนนรวมสูงสุด

**สูตรการคำนวณ:**
```
Weight = 1 (ช่องว่าง) + (จำนวนตำแหน่งตัวเองในเส้นทาง / จำนวนช่องที่ต้องชนะ)
```

**การทำงาน:**
- **บุก (Invasion):** เส้นที่เรากำลังจะชนะจะให้คะแนนสูง → เพื่อปิดเกม
- **กัน (Defense):** เส้นที่ฝ่ายตรงข้ามกำลังจะชนะจะให้คะแนนสูง → เพื่อบล็อก
- **Priority:** ช่องกลางหรือใกล้กลางกระดานจะได้คะแนนพิเศษ → สามารถเดินเกมได้หลายทิศ

---

## 🔍 ขั้นตอนการคำนวณ

### ขั้นตอนที่ 1: หาความน่าจะเป็นจุดตัด (Axis Intersection)

#### 1.1 กำหนดเส้นที่เป็นไปได้ของแต่ละช่อง

**ตัวอย่าง: กระดาน 3×3, ชนะ 3**

ให้ระบุตำแหน่งเป็นตัวเลขแทนช่อง:

```
1 | 2 | 3
---------
4 | 5 | 6
---------
7 | 8 | 9
```

**เส้นทางที่เป็นไปได้:**
- **แนวนอน:** (1,2,3), (4,5,6), (7,8,9)
- **แนวตั้ง:** (1,4,7), (2,5,8), (3,6,9)
- **ทแยง:** (1,5,9), (3,5,7)

#### 1.2 นับจำนวนเส้นที่ช่องนั้นยังสามารถชนะได้

**กฎการนับ:**
- ถ้าเราเป็นผู้เริ่มเล่น → เลือกช่องที่มีความน่าจะเป็นชนะสูงสุด
- ถ้าเส้นมีคู่แข่งอยู่ → ตัดออก

**ตัวอย่าง:** ผู้เล่น X ลงในตำแหน่ง 5

```
1 | 2 | 3
---------
4 | X | 6
---------
7 | 8 | 9
```

**เส้นที่ถูกตัดออก (มี X อยู่แล้ว):**
- แนวนอน: (4,X,6)
- แนวตั้ง: (2,X,8)
- ทแยง: (1,X,9), (3,X,7)

**เส้นที่ยังสามารถชนะได้:**
- แนวนอน: (1,2,3), (7,8,9)
- แนวตั้ง: (1,4,7), (3,6,9)

**ช่องที่มีจุดตัดสูงสุด:**
- ช่อง 1, 3, 7, 9 (ผ่านหลายเส้น)

---

### ขั้นตอนที่ 2: คำนวณคะแนนความน่าจะเป็น (Probabilistic Weight)

#### 2.1 คำนวณ Weight ของแต่ละช่อง

ให้คะแนนตามโอกาสการชนะของแต่ละเส้นที่ช่องนั้นอยู่ โดยพิจารณา 3 ปัจจัย:

##### 2.1.1 การบุก (Invasion)
คำนวณคะแนนจากเส้นที่ตัวเองกำลังจะชนะ

**สูตร:**
```
Weight_Offense = 1 + (จำนวนตัวเองในเส้น / จำนวนช่องที่ต้องชนะ)
```

**ตัวอย่าง:** กระดาน 3×3, ชนะ 3
```
X | 2 | 3
---------
4 | O | 6
---------
7 | 8 | 9
```
- ถ้าลงช่อง 3: เส้น (X,2,3) มี X = 2 ตัว → Weight = 1 + (2/3) = **1.67**
- ถ้าลงช่อง 9: เส้น (X,O,9) มี X = 2 ตัว → Weight = 0 + (2/3) = **0.67**
- เลือกช่องที่ 3 **คะแนนรวมสูง** → โอกาสชนะมากกว่าช่อง 9

##### 2.1.2 การกัน (Defense)
คำนวณคะแนนจากเส้นที่ฝ่ายตรงข้ามกำลังจะชนะ เพื่อบล็อก

**สูตร:**
```
Weight_Defense = (จำนวนตัวคู่แข่งในเส้น / จำนวนช่องที่ต้องชนะ)
```

**ตัวอย่าง:** กระดาน 3×3, ชนะ 3
```
O | O | 3
---------
4 | X | 6
---------
7 | 8 | 9
```
- ถ้าลงช่อง 3: เส้น (O,O,3) ที่มี O = 2 ตัว → Weight = 1 + (2/3) = **1.67** 
- ถ้าลงช่อง 7: เส้น (O,4,7) ที่มี O = 1 ตัว → Weight = 1 + (1/3) = **1.33** 
- เลือกช่องที่ 3 **คะแนนรวมสูง** ต้องบล็อกด่วน เพราะคู่แข่งใกล้ชนะ

##### 2.1.3 ตำแหน่งกลางกระดาน (Priority)
ให้คะแนนพิเศษกับช่องที่อยู่กลางหรือใกล้กลางกระดาน

**สูตร:**
```
ระยะห่างจากจุดกลาง = √[(row - center_row)² + (col - center_col)²]
```

**ตัวอย่าง:** กระดาน 3×3, ชนะ 3
```
(0,0) | (0,1) | (0,2)     →     1 | 2 | 3
(1,0) | (1,1) | (1,2)     →     4 | 5 | 6
(2,0) | (2,1) | (2,2)     →     7 | 8 | 9
```
**คำนวณระยะห่าง:** 
```
ช่อง 5 (1,1): √[(1-1)² + (1-1)²] = √[0 + 0] = 0
ช่อง 2 (0,1): √[(0-1)² + (1-1)²] = √[1 + 0] = 1
ช่อง 4 (1,0): √[(1-1)² + (0-1)²] = √[0 + 1] = 1
ช่อง 1 (0,0): √[(0-1)² + (0-1)²] = √[1 + 1] = √2 ≈ 1.414
ช่อง 3 (0,2): √[(0-1)² + (2-1)²] = √[1 + 1] = √2 ≈ 1.414
```
**สูตร:**
```
Weight_Priority = 1 / (ระยะห่างจากจุดกลาง + 1)
```

**ตัวอย่าง:** กระดาน 3×3 (จุดกลาง = ตำแหน่ง 5)
```
1 | 2 | 3
---------
4 | 5 | 6
---------
7 | 8 | 9
```
- ช่อง 5 (กลาง): ระยะห่าง = 0 → Weight = 1/(0+1) = **1.0**
- ช่อง 2,4,6,8 (ติดกลาง): ระยะห่าง ≈ 1 → Weight = 1/(1+1) = **0.5**
- ช่อง 1,3,7,9 (มุม): ระยะห่าง ≈ 1.4 → Weight = 1/(1.4+1) = **0.42**

#### 2.2 รวมคะแนนทั้งหมด

รวมคะแนนจากทุกเส้นที่ช่องนั้นอยู่ (Offense + Defense + Priority)

**สูตรรวม:**
```
Total_Weight = Σ(Weight_Offense + Weight_Defense) + Weight_Priority
```

เลือกช่องที่ **คะแนนรวมสูงสุด** → โอกาสชนะมากที่สุด

#### 2.3 ตัวอย่างการคำนวณ Weight แบบสมบูรณ์

**สถานการณ์:** กระดาน 3×3, ชนะ 3

```
O | 2 | 3
---------
4 | X | 6
---------
7 | 8 | 9
```

**คำนวณคะแนน O ถ้าลงช่อง 3:**
- **Invasion:** เส้น (O,2,3) มี O = 1 → Weight = 1 + (1/3) = 1.33
- **Defense:** เส้น (2,X,8) มี X = 1 → Weight = 0 + (1/3) = 0.33
- **Priority:** ระยะห่างจากกลาง ≈ 1 → Weight = 0.5
- **รวม:** 1.33 + 0.33 + 0.5 = **2.16**

**คำนวณคะแนน O ถ้าลงช่อง 9:**
- **Invasion:** เส้น (7,8,9) ว่างเปล่า → Weight = 1 + (0/3) = 1.0
- **Defense:** เส้น (1,X,9) มี X = 1 → Weight = 0 + (1/3) = 0.33
- **Priority:** ระยะห่างจากกลาง ≈ 1.4 → Weight = 0.42
- **รวม:** 1.0 + 0.33 + 0.42 = **1.75**

**ผลลัพธ์:** ลงช่อง 3 (คะแนน 2.16) สูงกว่าช่อง 9 (คะแนน 1.75) → AI เลือกลงช่อง 3

---

## 📋 สรุปกลยุทธ์ Bot

### ลำดับความสำคัญ:

1. **Axis Intersection**  
   เลือกช่องที่อยู่บนเส้นทางชนะได้หลายเส้น

2. **Probabilistic Weight**  
   คำนวณคะแนนรวมจากการบุก/ป้องกัน รวมถึงการบล็อกคู่แข่งและการให้ความสำคัญกับตำแหน่งกลางกระดาน


---

## 🎯 ระดับความยากของ Bot

| ระดับ | กลยุทธ์ |
|-------|---------|
| **EASY** | เดินแบบสุ่มในช่องว่าง + Soft-Block พื้นฐาน |
| **NORMAL** | ใช้ Soft-Block + Weight แบบพื้นฐาน |
| **HARD** | ใช้ Soft-Block + Probabilistic Weight เต็มรูปแบบ + Blocking ล่วงหน้า |

---

## 📧 Contact

สำหรับคำถามหรือข้อเสนอแนะ กรุณาติดต่อ: [thanakrit7578@gmail.com]
